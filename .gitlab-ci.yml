image: maven:latest

stages:
  - build&test
  - package
  - dockerise

variables:
  MAVEN_OPTS: "-Dmaven.repo.local=.m2/repository"

cache:
  key: ${CI_COMMIT_REF_SLUG}
  paths:
    - .m2/repository

Build project:
  stage: build&test
  script:
    - mvn $MAVEN_OPTS compile



